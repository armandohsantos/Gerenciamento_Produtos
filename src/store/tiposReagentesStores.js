import { defineStore } from 'pinia'
import { ref } from 'vue'
import { supabase } from '../lib/supabaseClient'


export const useTiposReagentesStore = defineStore('tiposReagentes', () => {
    // declarar as variaveis - coluna do SB;
    // criar a função inserir  localização;
    // criar a função UPDATE localização;
    // criar a função DELETE localização;

    
} )